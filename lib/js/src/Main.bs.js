// Generated by BUCKLESCRIPT, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("bs-platform/lib/js/curry.js");
var Js_dict = require("bs-platform/lib/js/js_dict.js");
var Belt_Array = require("bs-platform/lib/js/belt_Array.js");
var Belt_Option = require("bs-platform/lib/js/belt_Option.js");
var State$Guacamole = require("./State.bs.js");
var VscodeImpl$Guacamole = require("./Editor/VscodeImpl.bs.js");

function Impl(Editor) {
  var State = State$Guacamole.Impl(Editor);
  var dict = { };
  var get = function (fileName) {
    return Js_dict.get(dict, fileName);
  };
  var getByEditor = function (editor) {
    var fileName = Curry._1(Editor.editorFileName, editor);
    return Js_dict.get(dict, fileName);
  };
  var add = function (fileName, state) {
    var match = Js_dict.get(dict, fileName);
    if (match !== undefined) {
      return /* () */0;
    } else {
      console.log("[ states ][ add ]");
      dict[fileName] = state;
      return /* () */0;
    }
  };
  var rename = function (oldName, newName) {
    var delete_ = (function (dict, key) {delete dict[key]});
    return Belt_Option.forEach(Js_dict.get(dict, oldName), (function (state) {
                  console.log("[ states ][ rename ]", oldName, newName);
                  delete_(dict, oldName);
                  return add(newName, state);
                }));
  };
  var destroy = function (fileName) {
    var delete_ = (function (dict, key) {delete dict[key]});
    return Belt_Option.forEach(Js_dict.get(dict, fileName), (function (state) {
                  console.log("[ states ][ destroy ]");
                  Curry._1(State.destroy, state);
                  return delete_(dict, fileName);
                }));
  };
  var contains = function (fileName) {
    return Belt_Option.isSome(Js_dict.get(dict, fileName));
  };
  var destroyAll = function (param) {
    Belt_Array.map(Js_dict.entries(dict), (function (param) {
            return Curry._1(State.destroy, param[1]);
          }));
    return /* () */0;
  };
  return {
          State: State,
          dict: dict,
          get: get,
          getByEditor: getByEditor,
          add: add,
          rename: rename,
          destroy: destroy,
          contains: contains,
          destroyAll: destroyAll
        };
}

var StateDict = {
  Impl: Impl
};

function Impl$1(Editor) {
  var State = State$Guacamole.Impl(Editor);
  var State$1 = State$Guacamole.Impl(Editor);
  var dict = { };
  var get = function (fileName) {
    return Js_dict.get(dict, fileName);
  };
  var getByEditor = function (editor) {
    var fileName = Curry._1(Editor.editorFileName, editor);
    return Js_dict.get(dict, fileName);
  };
  var add = function (fileName, state) {
    var match = Js_dict.get(dict, fileName);
    if (match !== undefined) {
      return /* () */0;
    } else {
      console.log("[ states ][ add ]");
      dict[fileName] = state;
      return /* () */0;
    }
  };
  var rename = function (oldName, newName) {
    var delete_ = (function (dict, key) {delete dict[key]});
    return Belt_Option.forEach(Js_dict.get(dict, oldName), (function (state) {
                  console.log("[ states ][ rename ]", oldName, newName);
                  delete_(dict, oldName);
                  return add(newName, state);
                }));
  };
  var destroy = function (fileName) {
    var delete_ = (function (dict, key) {delete dict[key]});
    return Belt_Option.forEach(Js_dict.get(dict, fileName), (function (state) {
                  console.log("[ states ][ destroy ]");
                  Curry._1(State$1.destroy, state);
                  return delete_(dict, fileName);
                }));
  };
  var contains = function (fileName) {
    return Belt_Option.isSome(Js_dict.get(dict, fileName));
  };
  var destroyAll = function (param) {
    Belt_Array.map(Js_dict.entries(dict), (function (param) {
            return Curry._1(State$1.destroy, param[1]);
          }));
    return /* () */0;
  };
  var States = {
    State: State$1,
    dict: dict,
    get: get,
    getByEditor: getByEditor,
    add: add,
    rename: rename,
    destroy: destroy,
    contains: contains,
    destroyAll: destroyAll
  };
  var partial_arg = /\.gcl$/i;
  var isGCL = function (param) {
    return partial_arg.test(param);
  };
  var addToSubscriptions = function (f, context) {
    context.subscriptions.push(f);
    return /* () */0;
  };
  var activate = function (context) {
    Curry._2(Editor.addToSubscriptions, Curry._1(Editor.onDidCloseEditor, destroy), context);
    Curry._2(Editor.addToSubscriptions, Curry._1(Editor.onDidChangeFileName, (function (oldName, newName) {
                return Belt_Option.forEach(oldName, (function (oldName) {
                              return Belt_Option.forEach(newName, (function (newName) {
                                            if (contains(oldName)) {
                                              if (Curry._1(isGCL, newName)) {
                                                return rename(oldName, newName);
                                              } else {
                                                return destroy(oldName);
                                              }
                                            } else {
                                              return 0;
                                            }
                                          }));
                            }));
              })), context);
    Curry._2(Editor.addToSubscriptions, Curry._1(Editor.onDidChangeActivation, (function (_previous, next) {
                return Belt_Option.forEach(Belt_Option.flatMap(next, get), (function (param) {
                              console.log("[activate]", param);
                              return /* () */0;
                            }));
              })), context);
    return Curry._2(Editor.addToSubscriptions, Curry._2(Editor.registerCommand, "load", (function (editor) {
                      var fileName = Curry._1(Editor.editorFileName, editor);
                      if (Curry._1(isGCL, fileName)) {
                        var match = get(fileName);
                        if (match !== undefined) {
                          console.log("[ main ][ LOAD ]");
                          return /* () */0;
                        } else {
                          console.log("[ main ][ first LOAD ]");
                          var state = Curry._2(State.make, context, editor);
                          return add(fileName, state);
                        }
                      } else {
                        return 0;
                      }
                    })), context);
  };
  var deactive = function (param) {
    return destroyAll(/* () */0);
  };
  return {
          State: State,
          States: States,
          isGCL: isGCL,
          addToSubscriptions: addToSubscriptions,
          activate: activate,
          deactive: deactive
        };
}

var Editor = {
  make: VscodeImpl$Guacamole.make,
  getExtensionPath: VscodeImpl$Guacamole.getExtensionPath,
  editorFileName: VscodeImpl$Guacamole.editorFileName,
  onDidChangeFileName: VscodeImpl$Guacamole.onDidChangeFileName,
  onDidChangeActivation: VscodeImpl$Guacamole.onDidChangeActivation,
  onDidCloseEditor: VscodeImpl$Guacamole.onDidCloseEditor,
  addToSubscriptions: VscodeImpl$Guacamole.addToSubscriptions,
  registerCommand: VscodeImpl$Guacamole.registerCommand,
  getGCLPath: VscodeImpl$Guacamole.getGCLPath,
  setGCLPath: VscodeImpl$Guacamole.setGCLPath
};

var State = State$Guacamole.Impl(Editor);

var State$1 = State$Guacamole.Impl(Editor);

var dict = { };

function get(fileName) {
  return Js_dict.get(dict, fileName);
}

function getByEditor(editor) {
  var fileName = VscodeImpl$Guacamole.editorFileName(editor);
  return Js_dict.get(dict, fileName);
}

function add(fileName, state) {
  var match = Js_dict.get(dict, fileName);
  if (match !== undefined) {
    return /* () */0;
  } else {
    console.log("[ states ][ add ]");
    dict[fileName] = state;
    return /* () */0;
  }
}

function rename(oldName, newName) {
  var delete_ = (function (dict, key) {delete dict[key]});
  return Belt_Option.forEach(Js_dict.get(dict, oldName), (function (state) {
                console.log("[ states ][ rename ]", oldName, newName);
                delete_(dict, oldName);
                return add(newName, state);
              }));
}

function destroy(fileName) {
  var delete_ = (function (dict, key) {delete dict[key]});
  return Belt_Option.forEach(Js_dict.get(dict, fileName), (function (state) {
                console.log("[ states ][ destroy ]");
                Curry._1(State$1.destroy, state);
                return delete_(dict, fileName);
              }));
}

function contains(fileName) {
  return Belt_Option.isSome(Js_dict.get(dict, fileName));
}

function destroyAll(param) {
  Belt_Array.map(Js_dict.entries(dict), (function (param) {
          return Curry._1(State$1.destroy, param[1]);
        }));
  return /* () */0;
}

var States = {
  State: State$1,
  dict: dict,
  get: get,
  getByEditor: getByEditor,
  add: add,
  rename: rename,
  destroy: destroy,
  contains: contains,
  destroyAll: destroyAll
};

var partial_arg = /\.gcl$/i;

function isGCL(param) {
  return partial_arg.test(param);
}

function addToSubscriptions(f, context) {
  context.subscriptions.push(f);
  return /* () */0;
}

function activate(context) {
  VscodeImpl$Guacamole.addToSubscriptions(VscodeImpl$Guacamole.onDidCloseEditor(destroy), context);
  VscodeImpl$Guacamole.addToSubscriptions(VscodeImpl$Guacamole.onDidChangeFileName((function (oldName, newName) {
              return Belt_Option.forEach(oldName, (function (oldName) {
                            return Belt_Option.forEach(newName, (function (newName) {
                                          if (contains(oldName)) {
                                            if (Curry._1(isGCL, newName)) {
                                              return rename(oldName, newName);
                                            } else {
                                              return destroy(oldName);
                                            }
                                          } else {
                                            return 0;
                                          }
                                        }));
                          }));
            })), context);
  VscodeImpl$Guacamole.addToSubscriptions(VscodeImpl$Guacamole.onDidChangeActivation((function (_previous, next) {
              return Belt_Option.forEach(Belt_Option.flatMap(next, get), (function (param) {
                            console.log("[activate]", param);
                            return /* () */0;
                          }));
            })), context);
  return VscodeImpl$Guacamole.addToSubscriptions(VscodeImpl$Guacamole.registerCommand("load", (function (editor) {
                    var fileName = VscodeImpl$Guacamole.editorFileName(editor);
                    if (Curry._1(isGCL, fileName)) {
                      var match = get(fileName);
                      if (match !== undefined) {
                        console.log("[ main ][ LOAD ]");
                        return /* () */0;
                      } else {
                        console.log("[ main ][ first LOAD ]");
                        var state = Curry._2(State.make, context, editor);
                        return add(fileName, state);
                      }
                    } else {
                      return 0;
                    }
                  })), context);
}

function deactive(param) {
  return destroyAll(/* () */0);
}

exports.StateDict = StateDict;
exports.Impl = Impl$1;
exports.State = State;
exports.States = States;
exports.isGCL = isGCL;
exports.addToSubscriptions = addToSubscriptions;
exports.activate = activate;
exports.deactive = deactive;
/* State Not a pure module */
